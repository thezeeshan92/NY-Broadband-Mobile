<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface">

    <!-- Back -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:titleTextColor="?attr/colorOnSurface" />

    <!-- Scrollable content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/screen_margin"
            android:paddingTop="24dp">

            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/onboarding_signin_title"
                android:textSize="@dimen/text_headline_large"
                android:textStyle="bold"
                android:textColor="?attr/colorOnSurface"
                android:lineSpacingMultiplier="1.15" />

            <TextView
                android:id="@+id/tvBody"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/onboarding_signin_body"
                android:textSize="@dimen/text_body"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:lineSpacingMultiplier="1.4" />

            <!-- Divider -->
            <com.google.android.material.divider.MaterialDivider
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:layout_marginBottom="24dp" />

            <!--
                Google Sign-In button.
                TODO: wire to Firebase Auth GoogleSignInClient in the next iteration.
                      Real implementation: FirebaseAuth.getInstance().signInWithCredential(...)
            -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSignInGoogle"
                android:layout_width="match_parent"
                android:layout_height="@dimen/button_height_large"
                android:text="@string/onboarding_signin_google"
                android:textSize="15sp"
                style="@style/Widget.Material3.Button.OutlinedButton" />

            <!--
                Guest / anonymous flow.
                TODO: call FirebaseAuth.getInstance().signInAnonymously() in the next iteration.
            -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnGuest"
                android:layout_width="match_parent"
                android:layout_height="@dimen/button_height_large"
                android:layout_marginTop="12dp"
                android:text="@string/onboarding_signin_guest"
                android:textSize="15sp"
                style="@style/Widget.Material3.Button.TextButton" />

            <!-- Privacy note -->
            <TextView
                android:id="@+id/tvTerms"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="@string/onboarding_signin_terms"
                android:textSize="@dimen/text_caption"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:gravity="center"
                android:lineSpacingMultiplier="1.3" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</LinearLayout>
